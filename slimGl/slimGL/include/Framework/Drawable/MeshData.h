#include <glm/glm.hpp>

struct MeshData
{
	//! the position of the mesh relatively to the origin of the whole model
	glm::vec3 position;
	//! the rotation of the mesh relatively to the origin of the whole model
	glm::vec3 rotation;
};
